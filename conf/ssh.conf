include classpath("application.conf")


vampires {

  workload {
    task = "sleep 5"
    start = 0
    stop = 10
  }

  start  = [
    {
      provider = ssh
      type = local
      count = 2
    }
  ]

}
