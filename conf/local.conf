include classpath("application.conf")

vampires {

//  mode = sampling

  workload {
    task = "sleep 4"
    sequenceStart = 1
    sequenceStop = 100
  }

  executors = ["docker", "fork"]
  start  = [
    {
      provider = local
      type = local
      count = 1
    }
  ]

}
