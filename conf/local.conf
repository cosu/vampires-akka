include classpath("/application.conf")

vampires {

  workload {
    task = "sleep 10"
    start = 0
    stop = 5
  }

  executors = ["docker", "fork"]
  start  = [
    {
      provider = local
      type = local
      count = 1
    }
  ]

}
