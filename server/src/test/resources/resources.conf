vampires {
  resources {
    enabled = ["vampires.resources.ssh"]
    ssh {
      enabled = ["vampires.resources.ssh.local"]
      local {
        address =  "localhost"
        count = 1
      }
    }
  }
}